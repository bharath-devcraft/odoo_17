<odoo>
    <data>
    <record id="cm_kyc_form" model="ir.ui.view">
        <field name="name">Cm KYC Form</field>
        <field name="model">cm.kyc</field>
        <field name="arch" type="xml">
        <form string="KYC">
            
                    <header>
                        <button string="Approve" 
                            class="btn btn-success" 
                            icon="fa-thumbs-up" 
                            type="object" 
                            name="entry_approve" 
                            invisible = "status not in ('draft','editable')" 
                            confirm="Are you sure to proceed ?"/>
                        <button string="Set To Draft"
                            class="btn btn-primary"
                            type="object" name="entry_draft"
                            invisible = "status != 'active'" 
                            confirm="Are you sure to proceed ?"/>
                        <button string="Inactive"
                            class="btn btn-danger"
                            icon="fa-thumbs-down" 
                            type="object" name="entry_inactive" 
                            invisible = "status != 'active'" 
                            confirm="Are you sure to proceed ?"/>
                        <field name="status" widget="statusbar" style="width:700px;"/>                
                    </header>
                    
                    <sheet>                    
                        <widget name="web_ribbon" title="Active" bg_color="bg-success" invisible = "status != 'active'"/>
                        <group>
                            <group >
                                <field name="model_id" class="name-value" string="Form Name" required="1" readonly = "status in ('active','inactive')" options="{'no_create': True}"/>
                                <field name="country_id"  required="1"  readonly = "status in ('active','inactive')" options="{'no_create': True}"/>
                            </group>
                            <group>
                                <field name="eff_from_date" required="1" readonly = "status in ('active','inactive')"/>
                                <field name="field_ids" required="1"  readonly = "status in ('active','inactive')" domain="[('model_id', '=', model_id)]" widget="many2many_tags" options="{'no_create': True}"/>
                                <field name="status" readonly = "1" invisible="1"/>
                            </group>
                        </group>
                        
                        <notebook> 
                            <page string="Document Details" >
                                <field name="line_ids" colspan="8" nolabel="1" readonly = "status in ('active','inactive')">
                                    <tree string="Document Details" editable="top">
                                        <field name="header_id" column_invisible="1"/>
                                        <field name="name" required="1"/>
                                        <field name="mandatory" required="1"/>
                                        <field name="validity" required="1" colspan="2"/>
                                        <field name="validity_period" required="validity == 'limited'"/> 
                                        <field name="remark"/>                                                                                                             
                                        <field name="company_id" column_invisible="1"/>                           
                                    </tree>
                                </field>
                            </page>
                            <page string="Attachments" >
                                <field name="line_ids_a" colspan="4" nolabel="1"  required="1" readonly = "status in ('active','inactive')">
                                    <tree string="Attachments" editable="top">
                                        <field name="header_id" column_invisible="1"/>
                                        <field name="attach_desc" string="Certificates" required="1"/>
                                        <field name="attachment_ids" required="1" widget="single_file_binary" options="{'no_create_edit': True}"/>
                                        <field name="attach_user_id" force_save="1" readonly="1"/>
                                        <field name="attach_date" force_save="1" readonly="1"/> 
                                        <field name="company_id" column_invisible="1"/>                              
                                    </tree>
                                </field>
                            </page>
                            <page string="Inactive Remarks" invisible = "status not in ('active','inactive')">
                                <field name="inactive_remark" readonly = "status != 'active'" nolabel="1"/>
                            </page>
                            <page string="Entry Info">
                                <group>
                                    <group>
                                        <field name="company_id"/>
                                        <field name="active_trans"/>
                                        <field name="active_rpt"/>
                                    </group>
                                    <group>
                                        <field name="active" widget="boolean_toggle"/>
                                        <field name="entry_mode"/>
                                    </group>
                                </group>
                                <group>
                                    <group>
                                        <field name="user_id" widget="many2one_avatar_user" />
                                        <field name="ap_rej_user_id" string="Approved By" widget="many2one_avatar_user" invisible = "status not in ('active','inactive')"/>
                                        <field name="inactive_user_id" widget="many2one_avatar_user" invisible = "status != 'inactive'" />
                                        <field name="update_user_id" widget="many2one_avatar_user"/>
                                    </group>
                                    <group>
                                        <field name="crt_date"/>
                                        <field name="ap_rej_date" string="Approved Date" invisible = "status not in ('active','inactive')"/>
                                        <field name="inactive_date" invisible = "status != 'inactive'"/>
                                        <field name="update_date"/>
                                    </group>
                                </group>
                            </page>
                        </notebook>
                        <group>
                            <field name="note"  placeholder="Write your notes here."/>
                        </group>
                        <div class="oe_chatter">
                            <field name="message_ids"/>
                        </div>  
                    </sheet>
                </form>
            </field>
        </record>

    

    <record id="cm_kyc_tree_js" model="ir.ui.view">
      <field name="name">Dashboard View</field>
      <field name="model">cm.kyc</field>
      <field name="priority" eval="10"/>
      <field name="arch" type="xml">
        <tree string="KYC" js_class="cm_kyc_list"
                decoration-info="status == 'draft'" 
                decoration-success = "status == 'editable'"
                decoration-bf = "status in ('active','draft')"
                decoration-primary = "status == 'inactive'">
            <field name="model_id" string="Form Name"/>
            <field name="eff_from_date"/>
            <field name="country_id"/>
            <field name="status" widget="badge" decoration-info = "status == 'draft'"
                decoration-success = "status == 'editable'"
                decoration-bf = "status == 'active'"
                decoration-primary = "status == 'inactive'"/>
                    <field name="ap_rej_user_id" string="Approved By"/>
                    <field name="crt_date"/>
                </tree>
            </field>
        </record>

    <record id="cm_kyc_tree" model="ir.ui.view">
      <field name="name">Tree View</field>
      <field name="model">cm.kyc</field>
      <field name="priority" eval="1"/>
      <field name="arch" type="xml">
                <tree string="KYC"
                decoration-info="status == 'draft'" 
                decoration-success = "status == 'editable'"
                decoration-bf = "status in ('active','draft')"
                decoration-primary = "status == 'inactive'">
            <field name="model_id" string="Form Name"/>
            <field name="eff_from_date"/>
            <field name="country_id"/>
            <field name="status" widget="badge" decoration-info = "status == 'draft'"
                decoration-success = "status == 'editable'"
                decoration-bf = "status == 'active'"
                decoration-primary = "status == 'inactive'"/>
                    <field name="ap_rej_user_id" string="Approved By"/>
           <field name="crt_date"/>
        </tree>
      </field>
    </record>

    
    <record id="cm_kyc_search" model="ir.ui.view">
        <field name="name">Search View</field>
        <field name="model">cm.kyc</field>
        <field name="arch" type="xml">
                <search string="KYC">
                <field name="model_id" string="Form Name" filter_domain="[('model_id','ilike',self)]"/>
                <field name="country_id" string="Country Name" filter_domain="[('country_id', 'ilike',self)]"/>
                <field name="field_ids"  filter_domain="[('field_ids', 'ilike', self)]"/>
                <filter name="my_master" string="My Master" domain="[('user_id', '=', uid)]"/> 
                <filter name="Draft" domain="[('status', '=', 'draft')]"/>
                <filter name="Editable" domain="[('status', '=', 'editable')]"/>
                <filter name="Active" domain="[('status', '=', 'active')]"/>
                <filter name="Inactive" domain="[('status', '=', 'inactive')]"/>
                <filter name="My Draft" string="My Draft" domain="[('user_id', '=', uid),('status', '=', 'draft')]"/>
                <filter name="My Editable" string="My Editable" domain="[('user_id', '=', uid),('status', '=', 'editable')]"/>
                <filter name="My Active" string="My Active" domain="[('user_id', '=', uid),('status', '=', 'active')]"/>
                <filter name="My Inactive" string="My Inactive" domain="[('user_id', '=', uid),('status', '=', 'inactive')]"/>
                <filter name="Hidden" domain="[('active', '=', False)]"/>
                
                <group expand="0" string="Group By">
                    <filter string="Country Name" name="country_id" domain="[]" context="{'group_by': 'country_id'}"/>
                    <filter name="eff_from_date" domain="[]" context="{'group_by': 'eff_from_date'}" />
                    <filter name="field_ids"  domain="[]" context="{'group_by': 'field_ids'}" />
                    <filter string="Status" name="status" domain="[]" context="{'group_by': 'status'}"/>
                    <filter string="Approved By" name="status" domain="[]" context="{'group_by': 'ap_rej_user_id'}"/>
                    <filter string="Approved Date" name="status" domain="[]" context="{'group_by': 'ap_rej_date'}"/>
                </group>
            </search>
        </field>
    </record>


    <record id="cm_kyc_win_action" model="ir.actions.act_window">
        <field name="name">KYC</field>
        <field name="res_model">cm.kyc</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[]</field>
        <field name="view_id" ref="cm_kyc_tree_js"/>
        <field name="search_view_id" ref="cm_kyc_search"/>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No master record found. Let's create one!</p>
        </field>
    </record>

    <menuitem name="KYC" id="cm_kyc_act_menu" parent="cm_customer.cm_crm_master_m_parent" action="cm_kyc_win_action" sequence="22"/>
    </data>
        
</odoo>
