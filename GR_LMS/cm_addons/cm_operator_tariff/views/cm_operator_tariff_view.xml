<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="cm_operator_tariff_form" model="ir.ui.view">
            <field name="name">Form View</field>
            <field name="model">cm.operator.tariff</field>
            <field name="arch" type="xml">
                <form string="Operator Tariff">
                    <header>
                        <button string="Approve" 
                            class="btn btn-success" 
                            icon="fa-thumbs-up" 
                            type="object" 
                            name="entry_approve" 
                            invisible = "status not in ('draft','editable')" 
                            confirm="Are you sure to proceed ?"/>
                        <button string="Set To Draft"
                            class="btn btn-primary"
                            type="object" name="entry_draft"
                            invisible = "status != 'active'" 
                            confirm="Are you sure to proceed ?"/>
                        <button string="Inactive"
                            class="btn btn-danger"
                            icon="fa-thumbs-down" 
                            type="object" name="entry_inactive" 
                            invisible = "status != 'active'" 
                            confirm="Are you sure to proceed ?"/>
                        <field name="status" widget="statusbar" style="width:700px;"/>                
                    </header>
                    
                    <sheet>                    
                        <widget name="web_ribbon" title="Active" bg_color="bg-success" invisible = "status != 'active'"/>
                        <group>
                            <group >
                                <label for="tank_operator_id" class="name-lable"/>
                                <div>
                                    <field name="tank_operator_id" class="name-value" options="{'no_create': True}" required="1" readonly = "status not in ('draft','editable')"/>
                                    <field name="name" widget="no_special_char" invisible="1" readonly = "status not in ('draft','editable')"/>
                                </div>
                                <field name="import_export" required="1" readonly = "status not in ('draft','editable')"/> 
                                <field name="currency_id" options="{'no_create': True}" required="1" readonly = "status not in ('draft','editable')"/> 
                                <field name="laden_exp_com" required="import_export in ('export','all')" invisible="import_export not in ('export','all')" readonly = "status not in ('draft','editable')"/> 
                                <field name="laden_imp_com" required="import_export in ('import','all')" invisible="import_export not in ('import','all')" readonly = "status not in ('draft','editable')"/> 
                                <field name="laden_ts_com" required="import_export in ('t_s','all')" invisible="import_export not in ('t_s','all')" readonly = "status not in ('draft','editable')"/> 
                                <field name="empty_exp_com" required="import_export in ('export','all')" invisible="import_export not in ('export','all')" readonly = "status not in ('draft','editable')"/> 
                                <field name="empty_imp_com" required="import_export in ('import','all')" invisible="import_export not in ('import','all')" readonly = "status not in ('draft','editable')"/> 
                                <field name="empty_ts_com" required="import_export in ('t_s','all')" invisible="import_export not in ('t_s','all')" readonly = "status not in ('draft','editable')"/> 
                            </group>
                            <group>
                                <field name="port_id" options="{'no_create': True}" required="1" readonly = "status not in ('draft','editable')"/> 
                                <field name="det_recovery"  required="1" readonly = "status not in ('draft','editable')"/>
                                <label for= "incent_val"/>
                                <div>
                                    <div class="o_address_format">
                                        <field name="incent_val" style="width: 35%%"  required="1" readonly = "status not in ('draft','editable')"/>
                                        <field name="currency_id"  style="width: 20%%"  options="{'no_create': True}" required="1" readonly = "status not in ('draft','editable')"/>
                                    </div>
                                </div>
                                <field name="tot_amt" readonly = "1" force_save="1"/>
                            </group>
                        </group>
                        
                        <notebook>
                            <page string="Details" >
                                <field name="line_ids" nolabel="1" readonly = "status not in ('draft','editable')">
                                    <tree editable="bottom">
                                        <field name="header_id" column_invisible="1"/>
                                        <field name="charges_id" required="1" options="{'no_create': True}"/>
                                        <field name="currency_id" required="1" options="{'no_create': True}"/>
                                        <field name="costing_type" required="1" width="110px"/>
                                        <field name="actual_cost" required="1" width="110px"/>
                                        <field name="gr_cost" required="1" width="130px"/>
                                        <field name="company_id" column_invisible="1"/>                           
                                    </tree>
                                </field>
                            </page>
                            <page string="Attachments" >
                                <field name="line_ids_a" nolabel="1" readonly = "status not in ('draft','editable')">
                                    <tree editable="bottom">
                                        <field name="header_id" column_invisible="1"/>
                                        <field name="attach_desc" required="1"/>
                                        <field name="attachment_ids" required="1" widget="single_file_binary" options="{'no_create_edit': True}"/>
                                        <field name="attach_user_id" force_save="1" readonly="1"/>
                                        <field name="attach_date" force_save="1" readonly="1"/> 
                                        <field name="company_id" column_invisible="1"/>                              
                                    </tree>
                                </field>
                            </page>
                            <page string="Inactive Remarks" invisible = "status not in ('active','inactive')">
                                <field name="inactive_remark" readonly = "status != 'active'" nolabel="1"/>
                            </page>
                            <page string="Entry Info">
                                <group>
                                    <group>
                                        <field name="company_id"/>
                                        <field name="active_trans"/>
                                        <field name="active_rpt"/>
                                    </group>
                                    <group>
                                        <field name="active" widget="boolean_toggle" groups="custom_properties.group_set_to_draft"/>
                                        <field name="entry_mode"/>
                                    </group>
                                </group>
                                <group>
                                    <group>
                                        <field name="user_id" widget="many2one_avatar_user" />
                                        <field name="ap_rej_user_id" string="Approved By" widget="many2one_avatar_user" invisible = "status not in ('active','inactive')"/>
                                        <field name="inactive_user_id" widget="many2one_avatar_user" invisible = "status != 'inactive'" />
                                        <field name="update_user_id" widget="many2one_avatar_user"/>
                                    </group>
                                    <group>
                                        <field name="crt_date"/>
                                        <field name="ap_rej_date" string="Approved Date" invisible = "status not in ('active','inactive')"/>
                                        <field name="inactive_date" invisible = "status != 'inactive'"/>
                                        <field name="update_date"/>
                                    </group>
                                </group>
                            </page>
                        </notebook>
                        <br></br>
                        <group>
                            <field name="remarks"  placeholder="Write your remarks here."/>
                        </group>
                        <div class="oe_chatter">
                            <field name="message_ids"/>
                        </div>  
                    </sheet>
                </form>
            </field>
        </record>

        <record id="cm_operator_tariff_tree_js" model="ir.ui.view">
            <field name="name">Dashboard View</field>
            <field name="model">cm.operator.tariff</field>
            <field name="priority" eval="10"/>
            <field name="arch" type="xml">
                <tree string="Operator Tariff" js_class="cm_operator_tariff_list"
                        decoration-info="status == 'draft'" 
                        decoration-success = "status == 'editable'"
                        decoration-bf = "status in ('active','draft')"
                        decoration-primary = "status == 'inactive'">
                    <field name="tank_operator_id"/>
                    <field name="import_export"/>
                    <field name="port_id"/>
                    <field name="currency_id"/>
                    <field name="tot_amt"/>
                    <field name="status" widget="badge" decoration-info = "status == 'draft'"
                        decoration-success = "status == 'editable'"
                        decoration-bf = "status == 'active'"
                        decoration-primary = "status == 'inactive'"/>
                    <field name="ap_rej_user_id" string="Approved By"/>
                    <field name="crt_date"/>
                </tree>
            </field>
        </record>

        <record id="cm_operator_tariff_tree" model="ir.ui.view">
            <field name="name">Tree View</field>
            <field name="model">cm.operator.tariff</field>
            <field name="priority" eval="1"/>
            <field name="arch" type="xml">
                <tree string="Operator Tariff"
                        decoration-info="status == 'draft'" 
                        decoration-success = "status == 'editable'"
                        decoration-bf = "status in ('active','draft')"
                        decoration-primary = "status == 'inactive'">
                    <field name="tank_operator_id"/>
                    <field name="import_export"/>
                    <field name="port_id"/>
                    <field name="currency_id"/>
                    <field name="tot_amt"/>
                    <field name="status" widget="badge" decoration-info = "status == 'draft'"
                        decoration-success = "status == 'editable'"
                        decoration-bf = "status == 'active'"
                        decoration-primary = "status == 'inactive'"/>
                    <field name="ap_rej_user_id" string="Approved By"/>
                    <field name="crt_date"/>
                </tree>
            </field>
        </record>

        
        <record id="cm_operator_tariff_search" model="ir.ui.view">
            <field name="name">Search View</field>
            <field name="model">cm.operator.tariff</field>
            <field name="arch" type="xml">
                <search string="Operator Tariff">
                    <field name="tank_operator_id" filter_domain="[('tank_operator_id','ilike',self)]"/>
                    <field name="port_id" filter_domain="[('port_id','ilike',self)]"/>
                    
                    <filter name="Draft" domain="[('status', '=', 'draft')]"/>
                    <filter name="Editable" domain="[('status', '=', 'editable')]"/>
                    <filter name="Active" domain="[('status', '=', 'active')]"/>
                    <filter name="Inactive" domain="[('status', '=', 'inactive')]"/>
                    <filter name="my_entries" string="My Entries" domain="[('user_id', '=', uid)]"/>
                    <filter name="My Draft" string="My Draft" domain="[('user_id', '=', uid),('status', '=', 'draft')]"/>
                    <filter name="My Editable" string="My Editable" domain="[('user_id', '=', uid),('status', '=', 'editable')]"/>
                    <filter name="My Active" string="My Active" domain="[('user_id', '=', uid),('status', '=', 'active')]"/>
                    <filter name="My Inactive" string="My Inactive" domain="[('user_id', '=', uid),('status', '=', 'inactive')]"/>
                    <filter name="Hidden" domain="[('active', '=', False)]"/>
                    
                    <group expand="0" string="Group By">
                        <filter string="Shipment Type" name="import_export" domain="[]" context="{'group_by': 'import_export'}"/>
                        <filter string="Status" name="status" domain="[]" context="{'group_by': 'status'}"/>
                        <filter string="Created By" name="user_id" domain="[]" context="{'group_by': 'user_id'}"/>
                        <filter string="Creation Date" name="crt_date" domain="[]" context="{'group_by': 'crt_date'}"/>
                        <filter string="Approved By" name="ap_rej_user_id" domain="[]" context="{'group_by': 'ap_rej_user_id'}"/>
                        <filter string="Approved Date" name="ap_rej_date" domain="[]" context="{'group_by': 'ap_rej_date'}"/>
                        <filter string="Inactivated By" name="inactive_user_id" domain="[]" context="{'group_by': 'inactive_user_id'}"/>
                        <filter string="Inactivated Date" name="inactive_date" domain="[]" context="{'group_by': 'inactive_date'}"/>
                    </group>
                </search>
            </field>
        </record>


        <record id="cm_operator_tariff_win_action" model="ir.actions.act_window">
            <field name="name">Operator Tariff</field>
            <field name="res_model">cm.operator.tariff</field>
            <field name="view_mode">tree,form</field>
            <field name="domain">[]</field>
            <field name="view_id" ref="cm_operator_tariff_tree_js"/>
            <field name="search_view_id" ref="cm_operator_tariff_search"/>
            <field name="context">{}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">No records found. Let's create one !</p>
            </field>
        </record>

        <menuitem name="Operator Tariff" id="cm_operator_tariff_act_menu" parent="cm_master.cm_tariff_master_m_parent" action="cm_operator_tariff_win_action" sequence="42"/>

    </data>
        
</odoo>
