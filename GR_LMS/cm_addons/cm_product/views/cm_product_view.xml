<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="cm_product_form" model="ir.ui.view">
            <field name="name">Form View</field>
            <field name="model">cm.product</field>
            <field name="arch" type="xml">
                <form string="Product">
                    <header>
                        <button string="Approve" 
                            class="btn btn-success" 
                            icon="fa-thumbs-up" 
                            type="object" 
                            name="entry_approve" 
                            invisible = "status not in ('draft','editable')" 
                            confirm="Are you sure to proceed ?"/>
                        <button string="Set To Draft"
                            class="btn btn-primary"
                            type="object" name="entry_draft"
                            invisible = "status != 'active'" 
                            confirm="Are you sure to proceed ?"/>
                        <button string="Inactive"
                            class="btn btn-danger"
                            icon="fa-thumbs-down" 
                            type="object" name="entry_inactive" 
                            invisible = "status != 'active'" 
                            confirm="Are you sure to proceed ?"/>
                        <field name="status" widget="statusbar" style="width:700px;"/>                
                    </header>
                    
                    <sheet>                    
                        <widget name="web_ribbon" title="Active" bg_color="bg-success" invisible = "status != 'active'"/>
                        <group>
                            <group >
                                <label for="name" class="name-lable"/>
                                <div>
                                    <field name="name" string= "Product / Trade Name" class="name-value" widget="no_special_char" required="1" readonly = "status not in ('draft','editable')"/>
                                </div> 
                                <field name="che_name" required="1" readonly = "status not in ('draft','editable')"/>
                                <field name="ship_name" required="1" force_save = "1" readonly = "1"/>
                                <field name="hs_id" string="HS/SAC Code" required="1" options="{'no_create': True}" readonly = "status not in ('draft','editable')"/>
                                <field name="bus_vert_id" options="{'no_create': True}" readonly = "status not in ('draft','editable')"/>
                                <field name="bus_vert_sub_type_id" options="{'no_create': True}" readonly = "status not in ('draft','editable')"/>
                                <field name="gra_den" required="1" readonly = "status not in ('draft','editable')"/>
                                <field name="acceptability" required="1" readonly = "status not in ('draft','editable')"/>
                                <field name="spl_req" readonly = "status not in ('draft','editable')"/>
                                
                                
                            </group>
                            <group>
                                <field name="dg_product" required="1" readonly = "status not in ('draft','editable')"/>
                                <field name="un_no" required="dg_product == 'yes'" invisible="dg_product != 'yes'" force_save="1" readonly = "status not in ('draft','editable')"/>
                                <field name="imo_class" widget="no_special_char" required="dg_product == 'yes'" invisible="dg_product != 'yes'" force_save="1"  readonly = "status not in ('draft','editable')"/>
                                <field name="sub_class1" widget="no_special_char" invisible="dg_product != 'yes'" readonly = "status not in ('draft','editable')"/>
                                <field name="sub_class2" widget="no_special_char" invisible="dg_product != 'yes'" readonly = "status not in ('draft','editable')"/>
                                <field name="psa_class" required="dg_product == 'yes'" invisible="dg_product != 'yes'" force_save="1" readonly = "status not in ('draft','editable')"/>
                                <field name="lpk_class" required="dg_product == 'yes'" invisible="dg_product != 'yes'" force_save="1" readonly = "status not in ('draft','editable')"/>
                                <field name="ems_class" required="dg_product == 'yes'" invisible="dg_product != 'yes'" force_save="1" readonly = "status not in ('draft','editable')"/>
                                <field name="pack_grp" required="dg_product == 'yes'" invisible="dg_product != 'yes'" force_save="1" readonly = "status not in ('draft','editable')"/>
                                <field name="tank_t_code" string = "Suitable Tank Type"  options="{'no_create': True}" required="1" readonly = "status not in ('draft','editable')"/>
                                <field name="mar_poll" required="dg_product == 'yes'" invisible="dg_product != 'yes'" readonly = "status not in ('draft','editable')"/>
                                <field name="fosfa" required="1" readonly = "status not in ('draft','editable')"/>
                                <field name="kosher" required="1" readonly = "status not in ('draft','editable')"/>
                                <field name="clean_cate_id" options="{'no_create': True}" required="1" readonly = "status not in ('draft','editable')"/>
                                <field name="status" readonly = "1" invisible="1"/>
                            </group>
                        </group>
                        
                        <notebook>
                            <page string="Attachments" >
                                <field name="line_ids" nolabel="1" readonly = "status not in ('draft','editable')">
                                    <tree editable="bottom">
                                        <field name="header_id" column_invisible="1"/>
                                        <field name="attach_desc" required="1"/>
                                        <field name="attachment_ids" required="1" widget="single_file_binary" options="{'no_create_edit': True}"/>
                                        <field name="attach_user_id" force_save="1" readonly="1"/>
                                        <field name="attach_date" force_save="1" readonly="1"/> 
                                        <field name="company_id" column_invisible="1"/>                              
                                    </tree>
                                </field>
                            </page>
                            <page string="Inactive Remarks" invisible = "status not in ('active','inactive')">
                                <field name="inactive_remark" readonly = "status != 'active'" nolabel="1"/>
                            </page>
                            <page string="Entry Info">
                                <group>
                                    <group>
                                        <field name="company_id"/>
                                        <field name="active_trans"/>
                                        <field name="active_rpt"/>
                                    </group>
                                    <group>
                                        <field name="active" widget="boolean_toggle"/>
                                        <field name="entry_mode"/>
                                    </group>
                                </group>
                                <group>
                                    <group>
                                        <field name="user_id" widget="many2one_avatar_user" />
                                        <field name="ap_rej_user_id" string="Approved By" widget="many2one_avatar_user" invisible = "status not in ('active','inactive')"/>
                                        <field name="inactive_user_id" widget="many2one_avatar_user" invisible = "status != 'inactive'" />
                                        <field name="update_user_id" widget="many2one_avatar_user"/>
                                    </group>
                                    <group>
                                        <field name="crt_date"/>
                                        <field name="ap_rej_date" string="Approved Date" invisible = "status not in ('active','inactive')"/>
                                        <field name="inactive_date" invisible = "status != 'inactive'"/>
                                        <field name="update_date"/>
                                    </group>
                                </group>
                            </page>
                        </notebook>
                        <group>
                            <field name="remarks"  placeholder="Write your remarks here."/>
                        </group>
                        <div class="oe_chatter">
                            <field name="message_ids"/>
                        </div>  
                    </sheet>
                </form>
            </field>
        </record>

        <record id="cm_product_tree_js" model="ir.ui.view">
            <field name="name">Dashboard View</field>
            <field name="model">cm.product</field>
            <field name="priority" eval="10"/>
            <field name="arch" type="xml">
                <tree string="Product" js_class="cm_product_list"
                        decoration-info="status == 'draft'" 
                        decoration-success = "status == 'editable'"
                        decoration-bf = "status in ('active','draft')"
                        decoration-primary = "status == 'inactive'">
                    <field name="name" string="Product / Trade Name"/>
                    <field name="che_name"/>
                    <field name="hs_id"/>
                    <field name="dg_product"/>
                    <field name="tank_t_code" string = "Suitable Tank Type" />
                    <field name="clean_cate_id"/>
                    <field name="status" widget="badge" decoration-info = "status == 'draft'"
                        decoration-success = "status == 'editable'"
                        decoration-bf = "status == 'active'"
                        decoration-primary = "status == 'inactive'"/>
                </tree>
            </field>
        </record>

        <record id="cm_product_tree" model="ir.ui.view">
            <field name="name">Tree View</field>
            <field name="model">cm.product</field>
            <field name="priority" eval="1"/>
            <field name="arch" type="xml">
                <tree string="Product"
                        decoration-info="status == 'draft'" 
                        decoration-success = "status == 'editable'"
                        decoration-bf = "status in ('active','draft')"
                        decoration-primary = "status == 'inactive'">
                    <field name="name" string="Product / Trade Name"/>
                    <field name="che_name"/>
                    <field name="hs_id"/>
                    <field name="dg_product"/>
                    <field name="tank_t_code" string = "Suitable Tank Type" />
                    <field name="clean_cate_id"/>
                    <field name="status" widget="badge" decoration-info = "status == 'draft'"
                        decoration-success = "status == 'editable'"
                        decoration-bf = "status == 'active'"
                        decoration-primary = "status == 'inactive'"/>
                </tree>
            </field>
        </record>

        
        <record id="cm_product_search" model="ir.ui.view">
            <field name="name">Search View</field>
            <field name="model">cm.product</field>
            <field name="arch" type="xml">
                <search string="Product">
                    <field name="name" string="Product / Trade Name" filter_domain="[('name','ilike',self)]"/>
                    <field name="che_name" string="Chemical Name" filter_domain="[('che_name','ilike',self)]"/>
                    <field name="hs_id" filter_domain="[('hs_id','ilike',self)]"/>
                    <field name="clean_cate_id" filter_domain="[('clean_cate_id','ilike',self)]"/>
                    
                    <filter name="my_entries" string="My Entries" domain="[('user_id', '=', uid)]"/> 
                    <filter name="Draft" domain="[('status', '=', 'draft')]"/>
                    <filter name="Editable" domain="[('status', '=', 'editable')]"/>
                    <filter name="Active" domain="[('status', '=', 'active')]"/>
                    <filter name="Inactive" domain="[('status', '=', 'inactive')]"/>
                    <filter name="My Draft" string="My Draft" domain="[('user_id', '=', uid),('status', '=', 'draft')]"/>
                    <filter name="My Editable" string="My Editable" domain="[('user_id', '=', uid),('status', '=', 'editable')]"/>
                    <filter name="My Active" string="My Active" domain="[('user_id', '=', uid),('status', '=', 'active')]"/>
                    <filter name="My Inactive" string="My Inactive" domain="[('user_id', '=', uid),('status', '=', 'inactive')]"/>
                    <filter name="Hidden" domain="[('active', '=', False)]"/>
                    
                    <group expand="0" string="Group By">
                        <filter string="HS/SAC Code" name="hs_id" domain="[]" context="{'group_by': 'hs_id'}"/>
                        <filter string="Dangerous Goods" name="dg_product" domain="[]" context="{'group_by': 'dg_product'}"/>
                        <filter string="Suitable Tank Type" name="tank_t_code" domain="[]" context="{'group_by': 'tank_t_code'}"/>
                        <filter string="Cleaning Category" name="clean_cate_id" domain="[]" context="{'group_by': 'clean_cate_id'}"/>
                        <filter string="Status" name="status" domain="[]" context="{'group_by': 'status'}"/>
                        <filter string="Approved By" name="ap_rej_user_id" domain="[]" context="{'group_by': 'ap_rej_user_id'}"/>
                        <filter string="Approved Date" name="ap_rej_date" domain="[]" context="{'group_by': 'ap_rej_date'}"/>
                    </group>
                </search>
            </field>
        </record>


        <record id="cm_product_win_action" model="ir.actions.act_window">
            <field name="name">Product</field>
            <field name="res_model">cm.product</field>
            <field name="view_mode">tree,form</field>
            <field name="domain">[]</field>
            <field name="view_id" ref="cm_product_tree_js"/>
            <field name="search_view_id" ref="cm_product_search"/>
            <field name="context">{}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">No records found. Let's create one!</p>
            </field>
        </record>

        <menuitem name="Product" id="cm_product_act_menu" parent="cm_master.cm_crm_master_m_parent" action="cm_product_win_action" sequence="29"/>

    </data>
        
</odoo>
