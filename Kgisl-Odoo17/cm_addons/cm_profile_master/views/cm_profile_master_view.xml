<odoo>
    <data>

    <!-- explicit form view definition -->
    <record id="cm_profile_master_form" model="ir.ui.view">
        <field name="name">Profile Master Form</field>
        <field name="model">cm.profile.master</field>
        <field name="arch" type="xml">
        <form string="Master">
            
            <header>
                <button string="Approve" class="btn btn-success"  icon="fa-thumbs-up" type="object" name="approve_warning_rule" invisible = "status not in ('draft','editable')" confirm="Are you sure to proceed ?"/>
                <button string="Set To Draft" class="btn btn-primary" type="object" name="entry_draft"  groups = "cm_user_mgmt.group_set_to_draft" invisible = "status != 'active'" confirm="Are you sure to proceed ?"/>
                <button string="Inactive" class="btn btn-danger" icon="fa-thumbs-down" type="object" name="entry_inactive" invisible = "status != 'active'" confirm="Are you sure to proceed ?"/>
                <field name="status" widget="statusbar" style="width:700px;"/>                
            </header>
            
            <sheet>
                <widget name="web_ribbon" title="Active" bg_color="bg-success" invisible = "status != 'active'"/>
                <group>
                    <group>
                       <label for="name" class="name-lable"/>
                        <div>
                            <field name="name" class ="name-class" required="1" readonly = "status in ('active','inactive')"/>
                        </div>
                    </group>
                    <group>
                        <field name="short_name" widget="upper_case" required="1" readonly = "status in ('active','inactive')"/>
                        <field name="status" readonly = "1" invisible="1"/>
                    </group>
                </group>
                
                <group string = "General Information">
                    <group>
                        <field name="contact_person" required="1" readonly = "status in ('active','inactive')"/>
                        <field name="mobile_no" required="1" readonly = "status in ('active','inactive')"/>
                        <field name="street" required="1" readonly = "status in ('active','inactive')"/>
                        <field name="street1" required="1" readonly = "status in ('active','inactive')"/>
                        <field name="landmark" readonly = "status in ('active','inactive')"/>
                        <field name="city_id" required="1" readonly = "status in ('active','inactive')" options="{'no_create': True}"/>
                        <field name="pincode" required="1" readonly = "status in ('active','inactive')"/>
                    </group>
                    <group>
                        <field name="phone_no" required="1" readonly = "status in ('active','inactive')"/>
                        <field name="email" required="1" readonly = "status in ('active','inactive')"/>
                        <field name="website" readonly = "status in ('active','inactive')"/>
                        <field name="fax" readonly = "status in ('active','inactive')"/>
                        <field name="state_id" required="1" readonly = "status in ('active','inactive')" options="{'no_create': True}"/>
                        <field name="country_id" required="1" readonly = "status in ('active','inactive')" options="{'no_create': True}"/>
                        <field name="currency_id" required="1" readonly = "status in ('active','inactive')" options="{'no_create': True}"/>
                    </group>
                </group>
                
                <group string = "Official Information" >
                    <group>
                        <field name="tds" readonly = "status in ('active','inactive')"/>
                        <field name="tan_no" widget="upper_case" readonly = "status in ('active','inactive')"/>
                        <field name="cst_no" readonly = "status in ('active','inactive')"/>
                        <field name="tin_no" widget="upper_case" readonly = "status in ('active','inactive')"/>
                        <field name="cheque_favor" readonly = "status in ('active','inactive')"/>
                    </group>
                    <group>
                        <field name="company_type" readonly = "status in ('active','inactive')"/>
                        <field name="grade" readonly = "status in ('active','inactive')"/>
                        <field name="pan_no" widget="upper_case" readonly = "status in ('active','inactive')"/>
                        <field name="aadhar_no" widget="numeric" readonly = "status in ('active','inactive')"/>
                        <field name="gst_category" required="1" readonly = "status in ('active','inactive')"/>
                        <field name="gst_no" widget="upper_case" required="gst_category == 'registered'" readonly = "status in ('active','inactive')"/>
                    </group>
                </group>

                
                <notebook>
                    <page string="Additional Contact Details" >
                        <field name="line_ids" string="Additional Contact Details" colspan="4" nolabel="1"  required="1" readonly = "status in ('active','inactive')">
                            <tree string="Additional Contact Details">
                                <field name="name" string="Contact Name"/>
                                <field name="title" placeholder="e.g. Mr." options="{'no_create': True}"/>
                                <field name="job_position" placeholder="e.g. Sales Director"/>
                                <field name="email"/>
                                <field name="phone_no"/>
                                <field name="mobile_no"/>
                            </tree>
                            <form string="Additional Contact Details"	>
                                <group>
                                    <field name="header_id" invisible="1"/>
                                    <field name="name" string="Contact Name" required="1"/>
                                    <field name="title" placeholder="e.g. Mr."/>
                                    <field name="job_position" placeholder="e.g. Sales Director"/>
                                    <field name="email" required="1"/>
                                    <field name="phone_no"/>
                                    <field name="mobile_no" required="1"/>
                                    <field name="note" placeholder="internal note..."/>
                                    <field name="attachment" widget="single_file_binary" options="{'no_create_edit': True}"/>
                                </group>

                            </form>
                        </field>
                    </page>
                    <page string="Bank Details" >
                        <field name="line_ids_c" string="Bank Details" colspan="4" nolabel="1"  required="1" readonly = "status in ('active','inactive')">
                            <tree string="Bank Details" editable="top">
                                <field name="header_id" column_invisible="1"/>
                                <field name="account_no" widget="numeric" required="1"/>
                                <field name="acc_holder_name" required="1"/>
                                <field name="bank_name" required="1"/>
                                <field name="branch_name" required="1"/>
                                <field name="ifsc_code"  widget="upper_case" required="1"/>                                
                            </tree>
                        </field>
                    </page>
                    <page string="Billing Address" >
                        <field name="line_ids_b" string="Billing Address" colspan="4" nolabel="1" mode="tree" widget="one2many_list" required="1" readonly = "status in ('active','inactive')">
                            <tree string="Billing address Tree">
                                <field name="effective_date" />
                                <field name="header_id" column_invisible="1"/>
                                <field name="name"/>
                                <field name="short_name"/>
                                <field name="gst_no"/>
                                <field name="street" string="Street" required="1"/>
                                <field name="street1" required="1"/>
                                <field name="city_id" options="{'no_create': True}"/>
                                <field name="state_id" options="{'no_create': True}"/>
                                <field name="country_id" required="1" options="{'no_create': True}"/>        
                                <field name="phone_no" required="1" />
                                <field name="email" required="1" />
                            </tree>
                            <form string="Billing Address"	>
                                <group>
                                    <group>
                                        <field name="name" required="1"/>
                                        <field name="effective_date" required="1"/>
                                        <field name="street" string="Street" required="1"/>
                                        <field name="street1" required="1"/>
                                        <field name="city_id" required="1" options="{'no_create': True}"/>
                                        <field name="state_id" required="1" options="{'no_create': True}"/>
                                        <field name="country_id" required="1" options="{'no_create': True}"/>        
                                        <field name="gst_no" widget="upper_case"/>
                                    </group>
                                    <group>
                                        <field name="short_name" widget="upper_case" required="1"/>
                                        <field name="pincode" required="1"/>                                        
                                        <field name="email" required="1" />
                                        <field name="phone_no" required="1" />
                                        <field name="fax"/>
                                        <field name="website" widget="url" placeholder="e.g. www.odoo.com"/>
                                        <field name="cin_no" widget="upper_case"/>
                                    </group>
                                </group>
                            </form>
                        </field>
                    </page>
                    <page string="Delivery Address" >
                        <field name="line_ids_d" string="Delivery Address" colspan="4" nolabel="1" mode="tree" widget="one2many_list" required="1" readonly = "status in ('active','inactive')">
                            <tree>
                                
                                <field name="effective_date" />
                                <field name="header_id" column_invisible="1"/>
                                <field name="name"/>
                                <field name="short_name"/>
                                <field name="gst_no"/>
                                <field name="street" string="Street" required="1"/>
                                <field name="street1" required="1"/>
                                <field name="city_id" required="0"/>
                                <field name="state_id" required="0"/>
                                <field name="country_id" required="1"/>        
                                <field name="phone_no" required="1" />
                                <field name="email" required="1" />
                                
                            </tree>
                            <form>
                                <group>
                                    <group>
                                        <field name="name" required="1"/>
                                        <field name="effective_date" required="1"/>
                                        <field name="street" string="Street" required="1"/>
                                        <field name="street1" required="1"/>
                                        <field name="city_id" required="1" options="{'no_create': True}"/>
                                        <field name="state_id" required="1" options="{'no_create': True}"/>
                                        <field name="country_id" required="1" options="{'no_create': True}"/>        
                                        <field name="gst_no" widget="upper_case"/>
                                    </group>
                                    <group>
                                        <field name="short_name" widget="upper_case" required="1"/>
                                        <field name="pincode" required="1"/>                                        
                                        <field name="email" required="1" />
                                        <field name="phone_no" required="1" />
                                        <field name="fax"/>
                                        <field name="website" widget="url" placeholder="e.g. www.openerp.com"/>
                                        <field name="cin_no" widget="upper_case"/>
                                    </group>
                                </group>
                            </form>
                        </field>
                    </page>
                    <page string="Attachments" >
                        <field name="line_ids_a" string="Attachments" colspan="4" nolabel="1"  required="1" readonly = "status in ('active','inactive')">
                            <tree string="Attachment Tree" editable="top">
                                <field name="header_id" column_invisible="1"/>
                                <field name="attach_desc" required="1"/>
                                <field name="attachment" required="1" widget="single_file_binary" options="{'no_create_edit': True}"/>
                                <field name="attach_user_id" force_save="1" readonly="1"/>
                                <field name="attach_date" force_save="1" readonly="1"/>                                
                            </tree>
                        </field>
                    </page>
                    <page string="Inactive Remark" invisible = "status not in ('active','inactive')">
                            <field name="inactive_remark" readonly = "status != 'active'" nolabel="1"/>
                    </page>
                    <page string="Entry Info">
                        <group>
                            <group>
                                <field name="active_trans"/>
                                <field name="active_rpt"/>
                            </group>
                            <group>
                                <field name="active" widget="boolean_toggle"/>
                                <field name="entry_mode"/>
                            </group>
                        </group>
                        <group>
                            <group>
                                <field name="user_id"/>
                                <field name="ap_rej_user_id" invisible = "status not in ('active','inactive')"/>
                                <field name="inactive_user_id" invisible = "status != 'inactive'" />
                                <field name="update_user_id"/>
                            </group>
                            <group>
                                <field name="crt_date"/>
                                <field name="ap_rej_date" invisible = "status not in ('active','inactive')"/>
                                <field name="inactive_date" invisible = "status != 'inactive'"/>
                                <field name="update_date"/>
                            </group>
                        </group>
                    </page>
                </notebook>
                <group>
                    <field name="note"  placeholder="Write your notes here."/>
                </group>
                <div class="oe_chatter">
                    <field name="message_ids"/>
                </div>  
            </sheet>
        </form>
        </field>
    </record>
    <!-- form view end -->
    
    <!-- explicit tree view with dash board definition -->

    <record id="cm_profile_master_tree_js" model="ir.ui.view">
      <field name="name">cm.profile.master.inherit.cm.profile.master.tree</field>
      <field name="model">cm.profile.master</field>
      <field name="priority" eval="10"/>
      <field name="arch" type="xml">
        <tree string="Master" js_class="cm_profile_master_list"
                decoration-info="status == 'draft'" 
                decoration-success = "status == 'editable'"
                decoration-bf = "status in ('active','draft')"
                decoration-primary = "status == 'inactive'">
            <field name="name"/>
            <field name="short_name"/>
            <field name="contact_person"/>
            <field name="mobile_no"/>
            <field name="email"/>
            <field name="status" widget="badge"  decoration-info = "status == 'draft'"
                decoration-success = "status == 'editable'"
                decoration-bf = "status == 'active'"
                decoration-primary = "status == 'inactive'"/>
           <field name="ap_rej_user_id"/>
        </tree>
      </field>
    </record>
    <!-- Tree view end -->
    <!-- explicit tree view without dash board definition -->

    <record id="cm_profile_master_tree" model="ir.ui.view">
      <field name="name">CM Profile Master Tree</field>
      <field name="model">cm.profile.master</field>
      <field name="arch" type="xml">
        <tree string="Master" 
                decoration-info="status == 'draft'" 
                decoration-success = "status == 'editable'"
                decoration-bf = "status in ('active','draft')"
                decoration-primary = "status == 'inactive'">
            <field name="name"/>
            <field name="short_name"/>
            <field name="contact_person"/>
            <field name="mobile_no"/>
            <field name="email"/>
            <field name="status" widget="badge"  decoration-info = "status == 'draft'"
                decoration-success = "status == 'editable'"
                decoration-bf = "status == 'active'"
                decoration-primary = "status == 'inactive'"/>
           <field name="ap_rej_user_id"/>
        </tree>
      </field>
    </record>
    <!-- Tree view end -->
    
    <!-- explicit search view definition -->
    <record id="cm_profile_master_search" model="ir.ui.view">
        <field name="name">CM Profile Master Search</field>
        <field name="model">cm.profile.master</field>
        <field name="arch" type="xml">
            <search string="Search Master">
                <field name="name" string="Name" filter_domain="[('name','ilike',self)]"/>
                <field name="short_name" string="Short Name" filter_domain="[('short_name','ilike',self)]"/>

                <filter name="my_master" string="My Master" domain="[('user_id', '=', uid)]"/>
                <filter name="Draft" domain="[('status', '=', 'draft')]"/>
                <filter name="Editable" domain="[('status', '=', 'editable')]"/>
                <filter name="Active" domain="[('status', '=', 'active')]"/>
                <filter name="Inactive" domain="[('status', '=', 'inactive')]"/>
                <filter name="My Draft" string="My Draft" domain="[('user_id', '=', uid),('status', '=', 'draft')]"/>
                <filter name="My Editable" string="My Editable" domain="[('user_id', '=', uid),('status', '=', 'editable')]"/>
                <filter name="My Active" string="My Active" domain="[('user_id', '=', uid),('status', '=', 'active')]"/>
                <filter name="My Inactive" string="My Inactive" domain="[('user_id', '=', uid),('status', '=', 'inactive')]"/>
                <filter name="Hidden" domain="[('active', '=', False)]"/>
                
                <group expand="0" string="Group By">
                    <filter string="Short Name" name="short_name" domain="[]" context="{'group_by': 'short_name'}"/>
                    <filter string="Status" name="status" domain="[]" context="{'group_by': 'status'}"/>
                    <filter string="Approved By" name="status" domain="[]" context="{'group_by': 'ap_rej_user_id'}"/>
                    <filter string="Approved Date" name="status" domain="[]" context="{'group_by': 'ap_rej_date'}"/>
                </group>
            </search>
        </field>
    </record>
    <!-- search view end -->

    <!-- actions opening views on models -->

    <record id="cm_profile_master_win_action" model="ir.actions.act_window">
        <field name="name">Profile Master</field>
        <field name="res_model">cm.profile.master</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[]</field>
        <field name="view_id" ref="cm_profile_master_tree_js"/>
        <field name="search_view_id" ref="cm_profile_master_search"/>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No master record found. Let's create one!</p>
        </field>
    </record>
    <!-- actions end -->

    <!-- actions menus -->

    <menuitem name="Profile Master" id="cm_profile_master_act_menu" parent="cm_master.cm_master_m_parent" action="cm_profile_master_win_action"/>

    </data>
        
</odoo>
