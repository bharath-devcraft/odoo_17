<odoo>
    <data>
       <!-- Form view starts -->
       
       
       <record id="m_form_v_company_form" model="ir.ui.view">
            <field name="name">res.company.form</field>
            <field name="model">res.company</field>
            <field name="arch" type="xml">
                <form string="Company">
		    
		    <header>
			<button string="Approve" class="btn btn-success" icon="fa-thumbs-up" type="object" name="entry_approve" invisible = "status not in ('draft','editable')" confirm="Are you sure to proceed ?"/>
			<button string="Set To Draft" class="btn btn-primary" type="object" name="entry_draft" groups = "cm_user_mgmt.group_set_to_draft" invisible = "status != 'active'" confirm="Are you sure to proceed ?"/>
			<button string="Inactive" class="btn btn-danger" icon="fa-thumbs-down" type="object" name="entry_inactive" invisible = "status != 'active'" confirm="Are you sure to proceed ?"/>
			<field name="status" widget="statusbar" style="width:700px;"/>                
		    </header>
		    
                  <sheet>
                    <field name="logo" widget='image' class="oe_avatar" options='{"zoom": true, "preview_image": "logo"}' />
<!--
                    <field name="logo" widget="image" class="oe_avatar"/>
-->
                   <group>    
                    <group>    
                   <field name="name" String ="Company Name" placeholder="Company Name" readonly = "status in ('active','inactive')"/>
                        
                   </group>
		    <group>    
                   <field name="code" String ="Code" required = "1" readonly = "status in ('active','inactive')"/>
                        
                   </group>
		   </group>
                    <notebook colspan="4">
                        <page string="General Information" name="general_info">
                            <group>
                                <group>
				    <field name="street" required = "1" placeholder="Street..." class="o_address_street" readonly = "status in ('active','inactive')"/>
				    <field name="street2" required = "1" string="Street1" placeholder="Street 1" class="o_address_street" readonly = "status in ('active','inactive')"/>
				    <field name="city" required = "1" placeholder="City" class="o_address_city" readonly = "status in ('active','inactive')"/>
				    <field name="state_id" required = "1" string="State" class="o_address_state" placeholder="State" options='{"no_open": True}' readonly = "status in ('active','inactive')"/>
				    <field name="country_id" required = "1" placeholder="Country" class="o_address_country" options='{"no_open": True, "no_create": True}' readonly = "status in ('active','inactive')"/>
				    <field name="zip" required = "1" placeholder="ZIP" class="o_address_zip" readonly = "status in ('active','inactive')"/>
					<field name="vat" readonly = "status in ('active','inactive')" invisible="1"/>
					<field name="company_registry" readonly = "status in ('active','inactive')" invisible="1"/>
                                    
				    <field name="currency_id" options="{'no_create': True, 'no_open': True}" id="company_currency" context="{'active_test': False}" readonly = "1"/>
                                </group>
                                <group>
                                    <field name="phone" string="Phone No" required = "1" class="o_force_ltr" readonly = "status in ('active','inactive')"/>
                                    <field name="mobile" string ="Mobile No" required = "1" class="o_force_ltr" readonly = "status in ('active','inactive')"/>
                                    <field name="email" required = "1" readonly = "status in ('active','inactive')"/>
                                    <field name="website" string="Website" widget="url" readonly = "status in ('active','inactive')"/>
                                    <field name="parent_id" groups="base.group_no_one" readonly = "status in ('active','inactive')"/>
                                    <field name="parent_id" invisible="1"/>
                                    <field name="sequence" invisible="1"/> 
                                    <field name="gst_no" required = "1" readonly = "status in ('active','inactive')"/>
                                    <field name="cin_no" readonly = "status in ('active','inactive')"/>                                                                       
                                    <field name="tin_no" readonly = "status in ('active','inactive')"/>
                                    <field name="vat_no" readonly = "status in ('active','inactive')" invisible="1"/>
                                    <field name="cst_no" readonly = "status in ('active','inactive')" invisible="1"/>                                    
				    <field name="color" widget="color_picker" readonly = "status in ('active','inactive')" invisible="1"/>
                                </group>
                                <group name="social_media"/>
                            </group>
                        </page>
                       <page string="Inactive Remark" invisible = "status in ('draft')">
			    <field name="inactive_remark" readonly = "status != 'active'" nolabel="1"/>
			</page>	
			<page string="Entry Info">
                        <group>
                            <group>
                                <field name="active_trans"/>
                                <field name="active_rpt"/>
                            </group>
                            <group>
                                <field name="active" widget="boolean_toggle"/>
                                <field name="entry_mode"/>
                            </group>
                        </group>
                        <group>
                            <group>
                                <field name="user_id"/>
                                <field name="ap_rej_user_id" invisible = "status not in ('active','inactive')"/>
                                <field name="inactive_user_id" invisible = "status != 'inactive'" />
                                <field name="update_user_id"/>
                            </group>
                            <group>
                                <field name="crt_date"/>
                                <field name="ap_rej_date" invisible = "status not in ('active','inactive')"/>
                                <field name="inactive_date" invisible = "status != 'inactive'"/>
                                <field name="update_date"/>
                            </group>
                        </group>
                    </page>
			
                    </notebook>
		    <group>
			<field name="note"  placeholder="Write your notes here."/>
		    </group>
                    </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers"/>
                    <field name="message_ids" widget="mail_thread"/>
                </div>
                </form>
              </field>
        </record>     
	
        <!-- Form view ends -->
        
        
        <!-- Tree view starts -->
        
        <record id="m_tree_view_company" model="ir.ui.view">
            <field name="name">Company Tree View</field>
            <field name="model">res.company</field>
            <field name="arch" type="xml">
		<tree string="Company" js_class="cm_company_list"
			decoration-info="status == 'draft'" 
			decoration-success = "status == 'editable'"
			decoration-bf = "status in ('active','draft')"
			decoration-primary = "status == 'inactive'">
		    <field name="name" string="Name"/>
		    <field name="code"/>
		    <field name="city"/>
		    <field name="state_id" string="State"/>
		    <field name="country_id"/>
		    <field name="mobile" string="Mobile No"/>
                    <field name="status" widget="badge" decoration-info = "status == 'draft'"
			decoration-success = "status == 'editable'"
			decoration-bf = "status == 'active'"
			decoration-primary = "status == 'inactive'"/>
		</tree>
            </field>
        </record>

        
        <!-- Tree view ends -->
        
	<!-- Search view starts -->

        <record id="m_search_view_company" model="ir.ui.view">
            <field name="name">Company Search View</field>
            <field name="model">res.company</field>
            <field name="arch" type="xml">
               <search string="Search Master">
		    <field name="name" string="Name" filter_domain="[('name','ilike',self)]"/>
		     
		    <filter name="my_master" string="My Master" domain="[('user_id', '=', uid)]"/> 
		    <filter name="Draft" domain="[('status', '=', 'draft')]"/>
		    <filter name="Editable" domain="[('status', '=', 'editable')]"/>
		    <filter name="Active" domain="[('status', '=', 'active')]"/>
		    <filter name="Inactive" domain="[('status', '=', 'inactive')]"/>
		    <filter name="My Draft" string="My Draft" domain="[('user_id', '=', uid),('status', '=', 'draft')]"/>
		    <filter name="My Editable" string="My Editable" domain="[('user_id', '=', uid),('status', '=', 'editable')]"/>
		    <filter name="My Active" string="My Active" domain="[('user_id', '=', uid),('status', '=', 'active')]"/>
		    <filter name="My Inactive" string="My Inactive" domain="[('user_id', '=', uid),('status', '=', 'inactive')]"/>
		    <filter name="Inactive List" domain="[('active', '=', False)]"/>
		    
		    <group expand="0" string="Group By">
			<filter string="Status" name="status" domain="[]" context="{'group_by': 'status'}"/>
			<filter string="Approved By" name="status" domain="[]" context="{'group_by': 'ap_rej_user_id'}"/>
			<filter string="Approved Date" name="status" domain="[]" context="{'group_by': 'ap_rej_date'}"/>			
		    </group>
            </search>
            </field>
        </record>
        
	<!-- Search view ends -->
		 
         <!-- action menu starts -->

        <record id="mma_res_company_new" model="ir.actions.act_window">
            <field name="name">Company</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">res.company</field>
             <field name="view_mode">tree,form</field>
            <field name="domain">[]</field>
            <field name="search_view_id" ref="m_search_view_company"/>
        </record>
        
         <!-- action menu ends -->
         
         <!-- action menu-tree starts -->

       <record id="res_company_new_tree" model="ir.actions.act_window.view">
            <field eval="1" name="sequence"/>
            <field name="view_mode">tree</field>
            <field name="view_id" ref="m_tree_view_company"/>
            <field name="act_window_id" ref="mma_res_company_new"/>
        </record>
        
         <!-- action menu-tree ends -->
         
         <!-- action menu-form starts -->

        <record id="res_company_new_form" model="ir.actions.act_window.view">
            <field eval="2" name="sequence"/>
            <field name="view_mode">form</field>
            <field name="view_id" ref="m_form_v_company_form"/>
            <field name="act_window_id" ref="mma_res_company_new"/>
        </record>


    <!-- actions menus -->

    <menuitem name="Company" id="cm_company_act_menu" parent="cm_core_m_parent" action="mma_res_company_new"/>

    </data>
</odoo>
