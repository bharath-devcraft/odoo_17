<odoo>
    <data>
    <!-- explicit form view definition -->
    <record id="cm_survey_form" model="ir.ui.view">
        <field name="name">Survey Form</field>
        <field name="model">cm.survey</field>
        <field name="arch" type="xml">
        <form string="Survey">
            
            <header>
                <button string="Approve" class="btn btn-success" icon="fa-thumbs-up" type="object" name="entry_approve" invisible = "status not in ('draft','editable')" confirm="Are you sure to proceed ?"/>
                <button string="Set To Draft" class="btn btn-primary" type="object" name="entry_draft" groups = "cm_user_mgmt.group_set_to_draft" invisible = "status != 'active'" confirm="Are you sure to proceed ?"/><!-- group need to set-->
                <button string="Inactive" class="btn btn-danger" icon="fa-thumbs-down" type="object" name="entry_inactive" invisible = "status != 'active'" confirm="Are you sure to proceed ?"/>
                <field name="status" widget="statusbar" style="width:700px;"/>                
            </header>
            
            <sheet>
                <widget name="web_ribbon" title="Active" bg_color="bg-success" invisible = "status != 'active'"/>
                <group>
                    <group>
                        <field name="name" required="1" readonly = "status in ('active','inactive','closed')"/>
                        <field name="purpose" required="1" readonly = "status in ('active','inactive','closed')"/>
                    </group>
                    <group>
                        <field name="from_date" required="1" readonly = "status in ('active','inactive','closed')"/>
                        <field name="to_date" required="1" readonly = "status in ('active','inactive','closed')"/>
                        <field name="access_token" readonly = "1" force_save="1" invisible="1"/>
                        <field name="status" readonly = "1" invisible="1"/>
                    </group>
                    <group>
                        <field name="survey_start_url" widget="CopyClipboardChar" invisible = "status != 'active'"/>
                    </group>    
                </group>
                
                <notebook>
                    <page string="Survey Details" >
                        <field name="line_ids" colspan="4" nolabel="1"  required="1" readonly = "1" force_save="1">
                            <tree string="Feedback" editable="top">
                                <field name="header_id" column_invisible="1"/>
                                <field name="name"/>
                                <field name="feedback"/>                            
                                <field name="received_date"/>                            
                            </tree>
                        </field>
                        <group>
                            <field name="note"  placeholder="Write your notes here."/>
                        </group>
                    </page>
                    <page string="Inactive Remark" invisible = "status not in ('active','inactive')">
                        <field name="inactive_remark" nolabel="1" readonly="status != 'active'"/>
                    </page>
                    <page string="Entry Info">
                        <group>
                            <group>
                                <field name="active_trans"/>
                                <field name="active_rpt"/>
                            </group>
                            <group>
                                <field name="active" widget="boolean_toggle"/>
                                <field name="entry_mode"/>
                            </group>
                        </group>
                        <group>
                            <group>
                                <field name="user_id" widget="many2one_avatar_user"/>
                                <field name="ap_rej_user_id" invisible = "status not in ('active','inactive','closed')" widget="many2one_avatar_user"/>
                                <field name="inactive_user_id" invisible = "status != 'inactive'" widget="many2one_avatar_user"/>
                                <field name="update_user_id" widget="many2one_avatar_user"/>
                            </group>
                            <group>
                                <field name="crt_date"/>
                                <field name="ap_rej_date" invisible = "status not in ('active','inactive','closed')"/>
                                <field name="inactive_date" invisible = "status != 'inactive'"/>
                                <field name="close_date" invisible = "status != 'closed'"/>
                                <field name="update_date"/>
                            </group>
                        </group>
                    </page>
                </notebook>
                <div class="oe_chatter">
                    <field name="message_ids"/>
                </div>  
            </sheet>
        </form>
        </field>
    </record>
    <!-- form view end -->

    
    <!-- explicit tree view definition -->

    <record id="cm_survey_tree" model="ir.ui.view">
      <field name="name">CM Survey Tree</field>
      <field name="model">cm.survey</field>
      <field name="arch" type="xml">
        <tree string="Survey" js_class="cm_survey_list"
                decoration-info="status == 'draft'" 
                decoration-success = "status == 'editable'"
                decoration-bf = "status in ('active','draft')"
                decoration-primary = "status == 'inactive'"
                decoration-muted = "status == 'closed'">

            <field name="name"/>
            <field name="purpose"/>
            <field name="from_date"/>
            <field name="to_date"/>
            <field name="status" widget="badge" decoration-info = "status == 'draft'"
                decoration-success = "status == 'editable'"
                decoration-bf = "status == 'active'"
                decoration-primary = "status == 'inactive'"
                decoration-muted = "status == 'closed'"/>
           <field name="ap_rej_user_id"/>
        </tree>
      </field>
    </record>
    <!-- Tree view end -->
    
    <!-- explicit search view definition -->
    <record id="cm_survey_search" model="ir.ui.view">
        <field name="name">CM Survey Search</field>
        <field name="model">cm.survey</field>
        <field name="arch" type="xml">
            <search string="Search Survey">
                <field name="name" string="Name" filter_domain="[('name','ilike',self)]"/>
                <field name="purpose" string="Purpose" filter_domain="[('purpose','ilike',self)]"/>
                 
                <filter name="my_survey" string="My Survey" domain="[('user_id', '=', uid)]"/> 
                <filter name="Draft" domain="[('status', '=', 'draft')]"/>
                <filter name="Active" domain="[('status', '=', 'active')]"/>
                <filter name="Inactive" domain="[('status', '=', 'inactive')]"/>
                <filter name="Editable" domain="[('status', '=', 'editable')]"/>
                <filter name="Closed" domain="[('status', '=', 'closed')]"/>
                <filter name="My Draft" domain="[('status', '=', 'draft'),('user_id', '=', uid)]"/>
                <filter name="My Active" domain="[('status', '=', 'active'),('user_id', '=', uid)]"/>
                <filter name="My Inactive" domain="[('status', '=', 'inactive'),('user_id', '=', uid)]"/>
                <filter name="My Editable" domain="[('status', '=', 'editable'),('user_id', '=', uid)]"/>
                <filter name="My Closed" domain="[('status', '=', 'closed'),('user_id', '=', uid)]"/>
                <filter name="Hidden" domain="[('active', '=', False)]"/>
                
                <group expand="0" string="Group By">
                    <filter string="Status" name="status" domain="[]" context="{'group_by': 'status'}"/>
                    <filter string="Approved By" name="status" domain="[]" context="{'group_by': 'ap_rej_user_id'}"/>
                    <filter string="Approved Date" name="status" domain="[]" context="{'group_by': 'ap_rej_date'}"/>
                </group>
            </search>
        </field>
    </record>
    <!-- search view end -->

    <!-- actions opening views on models -->

    <record id="cm_survey_win_action" model="ir.actions.act_window">
        <field name="name">Survey</field>
        <field name="res_model">cm.survey</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[]</field>
        <field name="search_view_id" ref="cm_survey_search"/>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No Survey record found. Let's create one!</p>
        </field>
    </record>
    <!-- actions end -->

    <menuitem name="Survey" id="cm_survey_act_menu" parent="custom_properties.custom_properties_general_sub_menu" action="cm_survey_win_action"/>

    </data>
        
</odoo>
